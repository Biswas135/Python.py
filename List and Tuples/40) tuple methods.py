tup = (1, 2, 1, 4)      # creating a tuple

print(tup.index(2))     # index() → returns the index of the first occurrence of 2
print(tup.count(1))     # count() → counts how many times 1 appears in the tuple
